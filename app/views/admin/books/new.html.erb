<!-- =========================================================
  View: Admin / Books 新規作成
  File: app/views/admin/books/new.html.erb
  ---------------------------------------------------------
  目的:
    - 新しい Book の作成フォームを表示
  構成:
    1) パンくず
    2) タイトル
    3) フォーム（_form 部分テンプレート）
========================================================= -->

<!-- =======================
     1) パンくず
======================= -->
<% breadcrumb :admin_book_new %>
<%= render "shared/breadcrumbs" %>

<!-- =======================
     2) タイトル
======================= -->
<% content_for :title, "Books / 新規作成" %>
<h1 class="text-xl font-semibold mb-4">Book を新規作成</h1>

<!-- =======================
     3) フォーム呼び出し
======================= -->
<%= render "form", book: @book %>
