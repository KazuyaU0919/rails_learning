<!-- app/views/admin_bootstrap/form.html.erb -->
<div class="mx-auto max-w-md p-6">
  <h1 class="text-xl font-semibold mb-4">一時昇格（管理者）</h1>
  <p class="text-sm text-slate-600 mb-4">
    Render の環境変数 <code>ADMIN_PROMOTE_TOKEN</code> に設定した値を入力して送信してください。
    ログイン中のあなた（<%= current_user&.email %>）が管理者になります。
  </p>

  <%= form_with url: "/_promote_me", method: :post, local: true, class: "space-y-3" do %>
    <div>
      <label class="block text-sm mb-1">トークン</label>
      <%= text_field_tag :token, "", class: "w-full border rounded px-3 py-2" %>
    </div>
    <%= submit_tag "昇格する", class: "px-4 py-2 rounded bg-slate-900 text-white" %>
  <% end %>
</div>
