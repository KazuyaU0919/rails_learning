<!-- =========================================================
  Page: クイズ一覧（/quizzes）
  ---------------------------------------------------------
  目的：
    - Rails Books と連動したクイズの一覧を表示する。
    - 先頭にパンくず、中央見出し、「🔰 初めての方へ」説明トグル、
      クイズカードのリスト、ページネーションを並べる。
========================================================= -->

<!-- =======================
     パンくずナビ
     - 画面上部の階層表示。共有パーシャルを使用。
======================= -->
<% breadcrumb :quizzes %>
<%= render "shared/breadcrumbs" %>

<!-- =======================
     見出しブロック（中央揃え）
======================= -->
<div class="mb-6 text-center">
  <h1 class="text-2xl font-semibold">Quizzes</h1>
  <p class="mt-1 text-sm text-slate-500">~ 教本連動クイズ ~</p>
</div>

<!-- =======================
     🔰 初めての方へ（折りたたみ解説）
     - Stimulus controller "reveal" で開閉
     - ボタンは右寄せ、本文は <pre> で改行維持
======================= -->
<section
  class="-mt-2 mb-4"
  data-controller="reveal"
  data-reveal-open-label-value="🔰 初めての方へ"
  data-reveal-close-label-value="🔰 説明を閉じる">
  <div class="flex justify-end">
    <button type="button"
            class="btn-toggle"
            data-reveal-target="button"
            data-action="reveal#toggle">🔰 初めての方へ</button>
  </div>
  <div class="hidden mt-2" data-reveal-target="content">
    <pre class="whitespace-pre-wrap leading-relaxed text-slate-700 bg-white ring-1 ring-slate-200 rounded-lg p-4 text-[15px]">
💡 クイズ機能へようこそ！！
この機能では、Rails Booksと連動した内容のクイズが出題されます。

Rails Booksで学習した内容を着実に覚えるための機能です。

Rails Booksで理解した内容を、
クイズ機能で何度も反復し、
内容を覚えられるまで挑戦しましょう。
    </pre>
  </div>
</section>

<!-- =======================
     クイズ一覧（カードのレンダリング）
     - quiz パーシャルをコレクションレンダリング
     - 視覚的な区切り（枠線・divider）付きのリスト
======================= -->
<ul class="divide-y divide-slate-200 rounded-lg border bg-white">
  <%= render partial: "quiz", collection: @quizzes, as: :quiz %>
</ul>

<!-- =======================
     ページネーション
     - Kaminari/Pagy など実装差異に配慮し、helper の存在確認を伴う
======================= -->
<div class="mt-4">
  <%= paginate @quizzes if respond_to?(:paginate) %>
</div>
